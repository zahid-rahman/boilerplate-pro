'use client';
export default function RootLayout({ children }: any) {
	/**
	 * set your auth related logic level hook
	 */

    /**
     * also use auth related context
     */
	return <>{children}</>;
}
